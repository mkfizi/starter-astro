---
const { 
    class: className = '', 
    ...rest
} = Astro.props;
---

<h4 {...rest} class={`font-medium text-black dark:text-white text-lg sm:text-xl lg:text-2xl ${className}`}>
    <slot />
</h4>