---
const { 
    class: className = '', 
    ...rest
} = Astro.props;
---

<h2 {...rest} class={`font-bold text-black dark:text-white text-2xl sm:text-3xl lg:text-4xl ${className}`}>
    <slot />
</h2>