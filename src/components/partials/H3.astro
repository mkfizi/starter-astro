---
const { 
    class: className = '', 
    ...rest
} = Astro.props;
---

<h3 {...rest} class={`font-semibold text-black dark:text-white text-xl sm:text-2xl lg:text-3xl ${className}`}>
    <slot />
</h3>